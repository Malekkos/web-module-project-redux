


const initialState = {
  favorites: [],
  displayFavorites: true //boolean
}

const favoriteReducer = (state = initialState, action) => {

  switch(action.type) {
    default:
            return state;
  }
}

export default favoriteReducer